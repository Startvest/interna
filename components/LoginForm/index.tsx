import { useForm } from "react-hook-form";
import { Input } from "../Input";
import { NextPage } from "next";
import styles from './login.module.css';

export function Form(){
return(
    <div>
        Login form would be created here
    </div>
)
}